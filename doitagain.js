function listCounter(array){
    for(let i = 0; i < array.length; i++) {
      const result = array[i];
      console.log(result);
    }
}

const arraylist = [2,5,6,7,12,5,5];

const treeList = ['apples','oranges','pears','mango'];

listCounter(treeList);